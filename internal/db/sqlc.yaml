version: 1
packages:
  - name: "db"
    path: "."
    queries: "./sql/queries.sql"
    schema: "./sql/schema.sql"
    engine: "postgresql"
    sql_package: "pgx/v4"
    emit_json_tags: true
    emit_interface: true
    emit_prepared_queries: true
